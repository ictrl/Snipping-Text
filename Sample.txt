from PIL import Image

pytesseract.pytesseract.tesseract cmd = r"C:\Program Files (x86) \Tesseract-OCR\te ract.exe"

   

# Path of working folder on Disk
src path = "C:/Users/cools/OneDrive/Desktop/imageToText/"*

 

ath) :
# Read image with op

def get strin

 

 

img = cv2.imread(img_path)

# Convert to gray
img = cv2.cvtColor(img, cv2.COLOR_BGR2GRAY)

# Apply dilation and erosion to remove some noise
kernel = np.ones((1, 1), np.uint8)

 
 

img = cv2.dilate(img, kernel, itorati
img = cv2.erode(img, kernel,

# Write image after removed noise
cv2.imwrite (src path + "removed img)

# Apply threshold to get image with only black and white
#img cv2.adaptiveThreshold (img, 255, cV2.ADARTIVE THRESH GAUSSIAN.C,. cv2.,IHRESH BINARY, 31.2)

# Write the image after apply opency to do some
cv2.imwrite(src bath + "thres.onc". ima)